{% extends "base.html.twig" %}

{% block title %}Creer une categorie{% endblock %}

{% block body %}
    <div class="row  mt-5 ">
        <div class="col-md-6 mx-auto">
            <h1>Creer une catégorie </h1>
            {{form_start(createCategorie)}}
                {{form_row(createCategorie.nom, {'attr' : {'placeholder':'Nom de la catégorie'}}) }}
                {{form_row(createCategorie.description, {'attr' : {'placeholder':'description'}}) }}
                {{form_row(createCategorie., {'attr' : {'placeholder':'description'}}) }}

                <button type="submit" class="btn btn-primary btn-block"> 
                
                    Ajouter une categorie
             
                </button>
            
            {{form_end(createCategorie)}}
        </div>
    </div>

{% endblock %}