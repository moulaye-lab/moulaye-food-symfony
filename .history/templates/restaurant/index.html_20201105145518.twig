{% extends 'base.html.twig' %}

{% block title %}Restaurants{% endblock %}

{% block body %}


 

<div class="jumbotron">
    
    <div class="col-md-12 mx-auto mt-5">
    <h1 class='text-decoration-underline'>Mes Restaurants</h1>
    <button class="btn btn-primary ">Ajouter Restaurant</button>
</div>

</div>

{% for row in restaurants|batch(3) %}
    <div class="row mt-2" >
       {% for restaurants in row %}
        <div class="col-md-4"> 
  
    
    <div class="card text-white bg-primary mb-3" style="max-width: 20rem;">
    <div class="card-header">{{restaurants.nom}}</div>
    <div class="card-body">
         
        <h4 class="card-title">{{restaurants.specialite}}</h4>
        <p class="card-text">{{restaurants.localisation}}</p>
        
        <button type="button"class="btn btn-success btn-lg btn-block">Editer</button>
    </div>
    </div>
    </div>

 {% endfor %}
 </div>
{% endfor %}

{% endblock %}

