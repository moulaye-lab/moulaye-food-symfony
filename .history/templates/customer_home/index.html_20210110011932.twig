{% extends 'base.html.twig' %}

{% block title %}BIENVENUE AU Restaurant {{resto.nom}}{% endblock %}

{% block body %}



<div class="mx-auto ml-100">
<p class="flotte">
<img src="{{ (resto.imageName ? vich_uploader_asset(resto,'imageFile') : asset('build/images/logo.jpg'))  }}"
      alt="{{resto.nom}} " width="200"/></p>
<p style="line-height:65px;">
 <h2 class='text-center'>   <p> BIENVENUE A {{resto.nom}} </p>
    <p>VOUS ÊTES ASSIS A LA TABLE {{table.numero}}</p>
   
     <p>SPECIALITE:{{resto.specialite}}</p>
</h2>

</p>
	
<div>
<div class="liens">
        <span class="row justify-content-center" >
          <a href="{{ path('app_infoRestaurant_customer',{'restaurant':resto.id} )}}"> <button class=" btn-secondary btn-lg">INFOS</button> </a>
         <a href="{{ path('customer_home_menu',{'restaurant':resto.id,'table':table.numero} )}}"><button class="btn-lg btn-primary">MENU </button></a> </span>
</div>
 





{% endblock %}

